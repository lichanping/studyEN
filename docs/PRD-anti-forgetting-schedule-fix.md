# PRD：抗遗忘复习排班修复

> 日期：2026-05-07
> 状态：待审核
> 优先级：高

---

## 1. 背景与问题

### 1.1 业务背景

抗遗忘复习系统基于艾宾浩斯遗忘曲线，在正课日期 D 后设置 10 个复习节点：D+1, D+2, D+3, D+5, D+7, D+9, D+12, D+14, D+17, D+21。

### 1.2 当前问题

| 问题 | 影响 |
|------|------|
| 排班逻辑错误 | 漏排 + 多排，复习任务与规则不符 |
| 数据源局限 | 仅依赖 localStorage，历史数据可能丢失 |
| 无法回溯 | 无法获取服务端已完成训练记录 |

### 1.3 复现案例

**用户：于熠凡**
**日期：2026-05-06 ~ 2026-05-07**

| 正课日期 | 应排复习日期 | 实排复习日期 | 问题 |
|----------|-------------|-------------|------|
| 4.22 | 4.23, 4.24, 4.25, 4.27, 4.29, 4.31, 5.4, 5.6, 5.9, 5.13 | 5.6, 5.4 | **漏排 4.23, 4.24, 4.25, 4.27, 4.29, 4.31, 5.9, 5.13** |
| 5.3 | 5.4, 5.5, 5.6, 5.8, 5.10, 5.12, 5.15, 5.17, 5.20, 5.24 | 5.6, 5.4 | **漏 5.5** |
| 5.4 | 5.5, 5.6, 5.7, 5.9, 5.11, 5.13, 5.16, 5.18, 5.21, 5.25 | 5.6, 5.7 | **漏 5.5** |
| 5.6 | 4.22(应排), 5.3, 5.4 | 仅 5.3, 5.4 | **漏 4.22** |
| 5.7 | 4.25, 5.4 | 4.22(多排), 5.1(多排), 5.3(多排), 4.25, 5.4 | **多排 4.22, 5.1, 5.3** |

---

## 2. 目标

1. **修复排班逻辑**：基于服务端已完成训练记录，正确计算复习计划
2. **提供核对界面**：Web 界面供用户查看和核对复习计划
3. **支持导出**：导出 CSV/Excel 格式用于线下核对

---

## 3. 功能需求

### 3.1 获取已完成训练记录

**功能**：从服务端 API 获取学员的已完成训练记录

**API 调用**：

```
GET https://apiv2.lxll.com/customer/training/orders?pageNumber=1&pageSize=100&status=COMPLETED
```

**请求头**：

```
Authorization: Bearer {token}
x-token-c: {token}
x-user-id: {userId}
x-ua: ct=2&v=5.0.96
accept: application/json, text/plain, */*
```

**响应处理**：

- 解析 `data.data` 数组
- 过滤出当前学员的训练记录
- 提取训练日期、课程类型等关键字段

**分页处理**：

- 初始 pageSize=100
- 若 total > pageSize，分页获取
- 缓存已获取数据，避免重复请求

### 3.2 复习计划计算

**核心算法**：

```javascript
const REVIEW_OFFSETS = [1, 2, 3, 5, 7, 9, 12, 14, 17, 21];

function calculateReviewSchedule(trainingRecords) {
    const reviewSchedule = new Map(); // date -> [reviewItems]

    trainingRecords.forEach(record => {
        const trainingDate = parseDate(record.trainingDate);

        REVIEW_OFFSETS.forEach(offset => {
            const reviewDate = addDays(trainingDate, offset);
            const reviewItem = {
                trainingDate: formatDate(trainingDate),
                reviewDate: formatDate(reviewDate),
                offset: offset,
                student: record.userName,
                courseType: record.courseType
            };

            if (!reviewSchedule.has(reviewDate)) {
                reviewSchedule.set(reviewDate, []);
            }
            reviewSchedule.get(reviewDate).push(reviewItem);
        });
    });

    return reviewSchedule;
}
```

**复习节点规则**：

| 节点序号 | 偏移天数 | 说明 |
|----------|---------|------|
| 1 | +1 | 正课次日 |
| 2 | +2 | 第 2 天 |
| 3 | +3 | 第 3 天 |
| 4 | +5 | 第 5 天 |
| 5 | +7 | 第 7 天 |
| 6 | +9 | 第 9 天 |
| 7 | +12 | 第 12 天 |
| 8 | +14 | 第 14 天 |
| 9 | +17 | 第 17 天 |
| 10 | +21 | 第 21 天 |

### 3.3 复习计划查看界面

**页面**：`anti-forgetting-schedule.html`

**功能模块**：

| 模块 | 功能 |
|------|------|
| 学员选择 | 下拉选择学员 |
| 日期范围 | 选择查看日期范围 |
| 复习计划列表 | 按日期展示复习任务 |
| 异常标记 | 标记漏排/多排 |
| 导出 | 导出 CSV/Excel |

**界面布局**：

```
┌─────────────────────────────────────────────────────────────┐
│  学员选择：[于熠凡 ▼]  日期范围：[2026-05-01] ~ [2026-05-31] │
│  [刷新计划] [导出CSV]                                        │
├─────────────────────────────────────────────────────────────┤
│  日期          | 应排复习来源        | 节点  | 状态          │
├─────────────────────────────────────────────────────────────┤
│  2026-05-06   | 4.22(+14), 5.3(+3) | 2     | ✅ 正常       │
│  2026-05-07   | 4.25(+12), 5.4(+3) | 2     | ✅ 正常       │
│  2026-05-08   | 5.3(+5), 5.4(+4?)  | -     | ⚠️ 异常检查    │
└─────────────────────────────────────────────────────────────┘
```

### 3.4 异常检测

**检测规则**：

| 异常类型 | 规则 | 标记 |
|----------|------|------|
| 漏排 | 应排未排 | 🔴 红色 |
| 多排 | 未排但应排 | 🟡 黄色 |
| 节点错误 | 偏移量不在 REVIEW_OFFSETS | 🔵 蓝色 |

---

## 4. 非功能需求

### 4.1 性能

- 首次加载：≤ 3s（含 API 调用）
- 刷新计划：≤ 1s（缓存命中）

### 4.2 兼容性

- 浏览器：Chrome, Safari, Edge
- 移动端：适配

### 4.3 数据安全

- Token 不持久化
- 本地缓存加密（可选）

---

## 5. 数据流

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  用户选择学员  │ ──→ │  检查缓存     │ ──→ │  缓存命中？   │
└──────────────┘     └──────────────┘     └──────┬───────┘
                                                   │
                                          ┌──────┴───────┐
                                          │     否         │
                                          ▼                ▼
                                    ┌──────────┐   ┌──────────┐
                                    │  调用API  │   │  计算复习  │
                                    │  获取数据  │   │  计划      │
                                    └────┬─────┘   └────┬─────┘
                                         │              │
                                         ▼              ▼
                                    ┌─────────────────────────┐
                                    │    缓存数据 + 展示结果    │
                                    └─────────────────────────┘
```

---

## 6. 实施计划

### 6.1 阶段一：API 验证

- [ ] 调用 `/customer/training/orders?status=COMPLETED` API
- [ ] 验证响应字段名
- [ ] 确认分页策略

### 6.2 阶段二：核心逻辑

- [ ] 实现 `calculateReviewSchedule()` 函数
- [ ] 单元测试
- [ ] 异常检测逻辑

### 6.3 阶段三：界面开发

- [ ] 新建 `anti-forgetting-schedule.html`
- [ ] 学员选择器
- [ ] 日期范围选择
- [ ] 复习计划列表
- [ ] 异常标记
- [ ] 导出功能

### 6.4 阶段四：测试验证

- [ ] 用例：于熠凡 2026-05-06 ~ 2026-05-07
- [ ] 验证：应排 vs 实排
- [ ] 回归测试

---

## 7. 验收标准

| 编号 | 标准 | 验证方式 |
|------|------|---------|
| AC-1 | 于熠凡 5.6 应排：4.22(+14), 5.3(+3), 5.4(+2) | 界面核对 |
| AC-2 | 于熠凡 5.7 应排：4.25(+12), 5.4(+3) | 界面核对 |
| AC-3 | 导出 CSV 格式正确 | 文件打开验证 |
| AC-4 | 刷新计划 ≤ 1s | 性能测试 |

---

## 8. 风险与缓解

| 风险 | 等级 | 缓解 |
|------|------|------|
| API 响应结构不符 | 高 | 阶段一先验证 |
| 历史数据缺失 | 中 | 明确保留策略 |
| 分页限制 | 中 | 实现分页获取 |

---

## 附录：复习节点计算示例

**正课日期：2026-04-22**

| 节点 | 偏移 | 复习日期 |
|------|------|---------|
| 1 | +1 | 2026-04-23 |
| 2 | +2 | 2026-04-24 |
| 3 | +3 | 2026-04-25 |
| 4 | +5 | 2026-04-27 |
| 5 | +7 | 2026-04-29 |
| 6 | +9 | 2026-05-01 |
| 7 | +12 | 2026-05-04 |
| 8 | +14 | 2026-05-06 |
| 9 | +17 | 2026-05-09 |
| 10 | +21 | 2026-05-13 |